# This is Git's per-user configuration file.
[user]
name = Adam Tolley
email = adam.tolley@gmail.com

[alias]
	pr = pull-request
	psh = ![[ $(git config "branch.$(git symbolic-ref --short HEAD).merge") = '' ]] && git push -u origin $(git symbolic-ref --short HEAD) || git push
